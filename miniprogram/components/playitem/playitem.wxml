<view class="play-item" bind:tap="gotoMusicList">
  <image class="img" src="{{playItem.picUrl}}"></image>
  <text class="play-count">{{count}}</text>
  <view class="name">{{playItem.name}}</view>
</view>